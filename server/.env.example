# ===== Jetour Password API 环境变量示例 =====
# 复制为 .env 并按需修改：copy .env.example .env
# （NSSM 方案直接在 install_service.bat 顶部修改 PORT / API_KEY 即可）

# 服务监听端口
PORT=8080

# 监听地址（默认 0.0.0.0）
# HOST=0.0.0.0

# API 接口密钥（请求头 X-API-Key 校验，生产环境务必修改为强密钥）
API_KEY=please-change-me-to-a-strong-secret
